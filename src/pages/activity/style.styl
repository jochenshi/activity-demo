.detail_activity{
  height 100%
  width 100%
  .detail_body{
    height 100%
    overflow-y auto
    .filter_area{
      text-align left
      border 1px solid #e3e3e3
      padding 10px
      font-size 12px
      display flex
      align-items center
      span{
        flex 1
      }
    }
  }
}
